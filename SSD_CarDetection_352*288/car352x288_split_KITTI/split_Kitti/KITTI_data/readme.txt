image_2 原图 1224*370等size的图像   7481张
label_2 标签 20类		    7481个

modify_annotations_txt.py： kitti当中有多个类别，消除掉与车无关的类别，另外在将几种车的类别都合并为car类型
	car_label 即仅有car类别 保存的结果  7481个

data_process.py：
	是否需要 将所有图片resize到一个尺寸
	将图片宽度切分成三块，与输入网络图像尽量相符合
		但切割中坐标点如何处理，正好在切线边缘处的车还要吗，坐标点如何变化

	思路：  否定
		先不resize到一个尺寸
		切线边缘处的车取消该点
		坐标点进行更改
	
	思路：  肯定
		resize到一个尺寸 1224 × 370
			only_one_size_image resize到1224*370的图像     6521张
			only_one_size_label resize到1224*370后的标签   6521个
			上诉两个文件夹去掉了没有车的图像
		坐标点进行更改，切线边缘处的车离那个分割线近，就保存到那幅图像
	操作：
		w*h   770张1224*370   6057张1242*375  296张1241*376    358张1238*374
		切割成(1224/3=408)*370  (1242/3=414)*375  (1241/3=413.6)*376 (1238/3=412.6)*374
		整除3不为整数，那不如resize到一个尺寸如1224*370，并且直接舍弃掉多出来的部分，将坐标点也需要对应回去

	image_split.py 就是操作resize到统一尺寸的图像进行分割 包括图像和坐标点
		split_image 分割完图像   12819张
		split_label 分割完标签   12819个

KITTI_xml.py 转化为xml格式标签  
	label 转化的xml格式文件 12819个
